Background figure photos — one per album (the images that float behind the carousel).
Order matches the eras. Run any through remove.bg and save the transparent PNG into
the app assets/figures/<id>.png for perfect cutouts.
